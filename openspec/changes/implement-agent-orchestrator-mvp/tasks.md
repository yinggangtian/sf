## 1. Implementation
- [ ] 1.1 建立模块骨架：`app/orchestrator/__init__.py`, `app/orchestrator/service.py`
- [ ] 1.2 槽位模型与校验：实现输入 Schema 与基本校验（时间 ≤ now, 数字 1..9, 性别枚举）
- [ ] 1.3 路由策略（最小）：优先 `algorithm_hint`，否则关键词回退 `xlr-liuren`
- [ ] 1.4 适配器调用：调用 `AlgorithmRegistry.get('xlr-liuren').run(inputs)` 并处理校验失败
- [ ] 1.5 RAG 可选：保留接口但默认返回空列表（不阻塞主流程）
- [ ] 1.6 解释输入包：组装 `{question,intent,slots,algorithm_id,plugin_result,rag_chunks?,profile?,history_summary?}`
- [ ] 1.7 错误与退出：槽位追问最多 1 轮，否则列出缺失字段并退出
- [ ] 1.8 日志与度量：记录一次执行的耗时与结果状态（成功/校验失败）
- [ ] 1.9 测试：`tests/test_agent_orchestrator.py` 覆盖 Happy Path + 槽位缺失 + 校验失败
- [ ] 1.10 文档：在 README 的 OpenSpec 部分标注本 change，并在合并后归档
